# Parity sweep 2 — findings tracker

Engineering record of the second adversarial parity review and fix sweep
(shipped in commit 3be3911). Kept for the per-item pi source references and
the documented deliberate divergences.

Second adversarial pass vs pi TS @130ae57 + npm build. 7 reviewers, ~70 findings.
Six top claims independently verified in main session (marked ✓v).
Severity: HIGH = wrong behavior/crash/hang; MED = real divergence, narrower; LOW = edge/cosmetic.
Rule: fix faithful to pi (npm build wins on source/build drift); every fix locked by a test.

## A. REGRESSIONS introduced by the first fix sweep (fix first)

- [x] ✓v **A1 HIGH compaction oscillates** — coding/compaction.go:218 `if !shouldCompact { return messages }` runs before the cached-summary check; after one compaction the next request's usage is small → full history returns. pi: compaction is permanent (persisted entry). Fix: once state.summary set, always re-apply checkpoint; only grow it. *Fixed: compact() restructured — cached checkpoint ALWAYS applied first (applyCheckpoint), shouldCompact (evaluated on the compacted view, like pi tokensBefore on buildSessionContext) only decides whether to EXTEND by re-summarizing messages[prefixLen:newCut] with the previous summary (pi boundaryStart = previous firstKeptEntryIndex). prefixLen indexes the ORIGINAL append-only transcript so it survives turns and re-compactions (clamped defensively if the transcript shrinks). Tests: TestCompactionPermanentAfterSmallUsage, TestCompactionExtendsWithPreviousSummary, TestCompactionStaysCompactedDuringToolLoop.*
- [x] ✓v **A2 HIGH serialMu deadlock** — agent/loop.go:469-477 Lock…emit(listeners)…Unlock without defer; a panicking listener leaks the mutex → all tool goroutines hang at wg.Wait. Fix: defer/func-scope the unlock so panic path releases. — FIXED: func-scoped critical section + deferred unlock in parallel thunk (listener ERROR still emitPanics after unlock); test TestAgentParallelToolsPanickingListenerDoesNotDeadlock, verified to hang pre-fix.
- [x] ✓v **A3 HIGH delta order reversed** — ai/providers/openai.go processes reasoning fields BEFORE content; pi openai-completions.ts:299-345 does content first. Block order + events flip when one chunk carries both. *Fixed: delta handler now runs content → reasoning → tool_calls → reasoning_details (pi :299-385); TestOpenAIDeltaOrderContentBeforeReasoning.*
- [x] **A4 MED finalize-at-finish_reason** — openai.go:260-276 emits end events inside the chunk handler: dup ends on repeated finish_reason, fixed type order (thinking→text→tools) vs pi's block order, ends emitted before the usage-only final chunk so Partial lacks usage; pi finalizes after the whole stream (:389-391) and also finalizes before throwing no-finish_reason. *Fixed: finalization moved out of the chunk handler — runs once after the SSE loop, in block order, Partial carries final usage, ends precede the no-finish_reason error; TestOpenAIFinalizeOnceAfterStreamWithUsage, TestOpenAIMissingFinishReasonEndsBlocksThenErrors.*
- [x] **A5 MED sawChoice over-correction** — openai.go:298 `sawChoice &&` guard makes a zero-choice stream (only [DONE]) emit clean done; pi throws "Stream ended without finish_reason" unconditionally. Drop sawChoice (usage-only final chunk doesn't trip pi's guard because finish_reason came earlier). *Fixed: sawChoice dropped; zero-choice stream errors unconditionally; TestOpenAIZeroChoiceStreamErrors.*
- [x] **A6 MED events.go over-de-omitempty** — ContentIndex/Delta/Content/Reason now unconditional on ALL variants → start/done/error carry spurious keys + `"reason":""`. pi is a discriminated union. Fix: per-variant marshal. *Fixed: AssistantMessageEvent.MarshalJSON emits exactly each pi union variant's fields (contentIndex:0 still present on block events); per-variant golden keyset test in events_test.go.*
- [x] **A7 MED same-model Responses ids mangled** — openai_responses.go:614-623 normalizes/truncates ids unconditionally; pi transform-messages.ts:133 normalizes only when !isSameModel — same-model replays raw ids (450+ chars OK). Truncation can break fc_↔rs_ pairing. *Fixed: normalization moved into the transformMessages closure (pre-computed per-source map since the Go callback lacks the source message) — same-model ids bypass it entirely and replay verbatim; assistant/toolResult branches now raw-split like pi (splitToolCallID also fixed to JS split semantics: second segment only); TestResponsesSameModelToolCallIDReplayedVerbatim, TestResponsesCrossModelToolCallIDStillNormalized.*

## B. Agent loop (beyond A2)

- [x] ✓v **B1 HIGH tool Execute panic kills run** — loop.go:595-609 no recover; pi agent-loop.ts:635-663 try/catch → error tool result, loop continues. Fix: recover (re-panic emitPanic), also emits matching tool_execution_end. — FIXED: recover in executePreparedToolCall (panic text = pi createErrorToolResult format); end still emitted via finalize; test TestAgentPanickingToolYieldsErrorResultAndContinues (parallel+sequential).
- [x] ✓v **B2 HIGH pendingToolCalls race** — agent.go:526 mutates the live map State() hands out; pi copies-on-write (new Set). -race confirmed crash-grade. Fix: copy-on-write. — FIXED: copy+mutate+swap on start/end (processEvent); test TestAgentStateSnapshotPendingToolCallsRace hammers State() iteration under -race.
- [x] **B3 MED Continue drops drained messages** — agent.go:339-353 drain then re-check active; concurrent Prompt wins → drained steers lost. Fix: claim run slot before draining or re-enqueue. — FIXED: claimRun(onClaimed) claims slot + drains under one lock (releaseRun on empty drain); pi error strings kept; test TestAgentContinueConcurrentPromptDoesNotLoseDrainedMessages.
- [x] **B4 LOW failure-path listener errs swallowed** (`_ = emit` agent.go:399-403); pi rejects. **B5 LOW** Metadata/WebSocketConnectTimeoutMs not forwarded into AgentLoopConfig. **B6 LOW** onUpdate listener error panics inside tool mid-flight; pi defers to after execute. **B7 LOW** turn_end ToolResults nil vs [] (and inconsistent with failure path). — ALL FIXED: B4 handleRunFailure returns first emit error, stops remaining events, run returns it after cleanup (TestAgentFailureEventListenerErrorStopsAndReturns); B5 Metadata+WebSocketConnectTimeoutMs plumbed AgentOptions→AgentLoopConfig→StreamOptions (TestAgentForwardsMetadataAndWebSocketTimeout); B6 onUpdate emit errors buffered, raised as emitPanic after Execute settles (TestAgentListenerErrorOnToolUpdateDefersUntilToolCompletes); B7 turn_end always emits []ai.ToolResultMessage{} (asserted in TestAgentErrorStopsLoop / TestAgentRunsToolCallThenFinishes).

## C. OpenAI completions (beyond A3-A5)

- [x] **C1 HIGH toolCallID normalizer dropped** — openai.go:321 passes nil to transformMessages; pi :753-768 normalizes pipe-ids/truncates >40 for provider==openai. Cross-provider replay 400s. *Fixed: normalizeOpenAIToolCallID (pipe-split → sanitize → 40-char clamp; non-pipe >40 only for provider==openai) passed to transformMessages; TestOpenAIToolCallIDNormalization (pipe, cross-model truncate, same-model pass-through, non-openai untouched).*
- [x] **C2 HIGH reasoning_details deltas unparsed** — pi :373-385 captures `reasoning.encrypted` → thoughtSignature; ours never populates it (send-side round-trip is dead code). OpenRouter encrypted reasoning breaks. *Fixed: delta.reasoning_details parsed; reasoning.encrypted entries matched to the tool call by id (content order) → thoughtSignature = compacted raw detail JSON (preserves field order vs JSON.stringify); round-trip test TestOpenAIReasoningDetailsStreamCapture + TestOpenAIReasoningDetailsNoMatchIgnored.*
- [x] **C3 HIGH tool-call by-id fallback lost** — pi keeps byIndex AND byId maps with `index===number?index:undefined`; ours int-map only — absent index ⇒ 0 ⇒ parallel calls merge. Fix: track both. *Fixed: Index is *int; ensureToolCallBlock keeps byIndex + byId maps, index-first lookup with id fallback, registered under both; TestOpenAIToolCallsByIdWithoutIndex, TestOpenAIToolCallIdFallbackMergesContinuation.*
- [x] **C4 HIGH cloudflare {VAR} baseUrl never resolved** (completions wired) — no resolveCloudflareBaseUrl port anywhere; catalog has literal `{CLOUDFLARE_ACCOUNT_ID}`. Affects completions+anthropic(+responses caller). Port cloudflare.ts env substitution. *Completions wired: isCloudflareProvider → resolveCloudflareBaseURL before onPayload (pi createClient order), error fails stream with pi's message; TestOpenAICloudflareBaseURLResolved/-MissingEnvFailsStream. Anthropic (E1) + responses callers still pending.*
- [x] **C5 MED prompt_cache_retention gated on SessionID** (completions done) (both completions :530-537 and responses :478-483); pi sets retention independent of sessionId. *Completions fixed: prompt_cache_retention=24h sent for long+supportsLongCacheRetention regardless of sessionId; prompt_cache_key still requires sessionId; TestOpenAICacheRetentionWithoutSessionID. Responses fixed alongside D2-D7: same split (retention independent of sessionId, key still gated); TestResponsesCacheRetentionWithoutSessionID.*
- [x] **C6 MED cached_tokens 0-vs-absent** — need *int to honor explicit 0 beating prompt_cache_hit_tokens fallback (pi ?? semantics). *Fixed: PromptTokensDetails.CachedTokens is *int with `?? prompt_cache_hit_tokens ?? 0` nullish chain; TestOpenAICachedTokensExplicitZero/-AbsentFallsBack.*
- [x] **C7 MED toolcall_delta for argless deltas not pushed; id/name overwrite vs first-wins** (pi :350-369). *Fixed: toolcall_delta pushed for every entry (empty delta string when no args); id/name first-wins; TestOpenAIToolCallDeltaPushedForArglessDeltas, TestOpenAIToolCallIdNameFirstWins.*
- [x] **C8 MED copilot dynamic headers absent** (completions wired) (X-Initiator/Openai-Intent/Copilot-Vision-Request) — also Anthropic (E2) and Responses callers. *Completions wired: buildCopilotDynamicHeaders merged after model.Headers, before affinity/options (pi :459-466 precedence); TestOpenAICopilotDynamicHeaders (user/agent initiator, vision, options override). E2 + responses still pending.*
- [x] **C9 MED user content shape** — multi-text joined "\n" instead of array parts; empty message sent instead of skipped (pi :796-816). *Fixed: array content always maps to array-of-parts (single text = 1-element array — pi's plain-string branch is unreachable since Go user messages always carry a content list); empty content skips the message without touching lastRole; TestOpenAIUserContentArrayParts. No existing test pinned the joined form.*
- [x] **C10 MED OnPayload err swallowed** (openai.go done) — all four providers `err == nil &&` pattern; pi propagates throw → error stream. Fix everywhere. *openai.go fixed: OnPayload error fails the stream; any non-nil return replaces the payload wholesale (pi `!== undefined`); TestOpenAIOnPayloadErrorFailsStream/-Replacement. Other providers handled by their agents.*
- [x] **C11 LOW** openrouter metadata.raw not appended; header precedence (affinity should override model.Headers); empty openRouterRouting {} truthy-sent; ant-ling fall-through; SSE skip-vs-error; registry Stream erases native opts. *Fixed: error.metadata.raw appended as "\n"+raw (TestOpenAIErrorMetadataRawAppended); header order model→copilot→affinity→options (TestOpenAIHeaderPrecedence); HasOpenRouterRouting presence flag sends explicit {} / omits null (TestOpenAIOpenRouterRoutingEmptyObjectSent); applyReasoningFormat rewritten as pi's exact else-if chain — ant-ling without effort falls through to generic off branch (TestOpenAIAntLingFallThrough); SSE skip-on-unparseable kept with deliberate-leniency comment; registry-Stream-erases-native-opts SKIPPED as documented Go API shape (note on RegisterOpenAICompletions).*

## D. OpenAI Responses (beyond A7)

- [x] **D1 HIGH retry defaults** — retry.go forces 2 (and coerces 0→2); pi maxRetries ?? 0, SDK matrix {408,409,429,≥500,x-should-retry,retry-after-ms, 8s cap+jitter, ignore RA>60s}. Decide: match pi default 0 via *int, align matrix; the >cap fail-fast came from excluded codex file — remove. *Fixed: retry.go defaults to 0 retries (no coercion; negatives clamp to 0 — int zero-value matches pi's `?? 0`, no *int needed). Matrix aligned with openai SDK: non-2xx only; x-should-retry true/false overrides status logic; 408/409/429/≥500; retry-after-ms (float ms) preferred over Retry-After (float secs or http-date); server delay honored only when 0<ms<60s (MaxRetryDelayMs overrides threshold), else IGNORED in favor of backoff min(0.5s*2^attempt, 8s) with 25% downward jitter (MaxRetryDelayMs <8s also caps backoff). >cap fail-fast removed. Network errors retry like 5xx; abort wins during backoff. Test-locked in retry_test.go.*
- [x] **D2 HIGH service_tier missing** — request param + flex×0.5/priority×2(/2.5 gpt-5.5) pricing (openai-responses.ts:251,283-291, shared:511-516). *Fixed: OpenAIResponsesOptions.ServiceTier sent as service_tier when set; pricing applied in response.completed with `response.service_tier ?? options.serviceTier` (response wins); priority ×2.5 only for exact id "gpt-5.5"; TestResponsesServiceTierParam, TestResponsesServiceTierPricing (6 cases).*
- [x] **D3 MED session cache headers missing** — session_id (compat.sendSessionIdHeader) + x-client-request-id; flag parsed but never read. *Fixed: with sessionId and cacheRetention != none, session_id sent when compat.SendSessionIDHeader, x-client-request-id always (pi :115,200-205); TestResponsesSessionCacheHeaders covers default/flag-off/retention-none.*
- [x] **D4 MED shortHash UTF-16 units** — charCodeAt per surrogate half vs our per-rune; astral ids diverge (test vectors exist). Also normalizeResponsesIDPart astral → 1 underscore vs 2. *Fixed: shortHash iterates utf16.Encode code units ("emoji 🙈 id" → jk0b7r1xq9646 + 6 more node-verified vectors); normalizeResponsesIDPart now per-UTF-16-unit (astral → two underscores, "ab🙈cd"→"ab__cd"); msg_ id >64 check also UTF-16 length; TestResponsesShortHashUTF16Vectors, TestResponsesNormalizeIDPartUTF16.*
- [x] **D5 MED post-stream error guard** — stopReason error/aborted must throw "An unknown error occurred" before done (pi :140-142); ours emits done. *Fixed: post-SSE guard fails the stream with that exact message; TestResponsesErrorStopReasonFailsStream (completed status "cancelled").*
- [x] **D6 MED non-allowed providers pipe-id merge** — !allowed ⇒ sanitize WHOLE id (pipe→_), no item id; ours splits raw. *Fixed via the A7 normalizer (whole-id sanitize for non-allowed providers → "call_1_fc_x", no "id" field; TestResponsesNonAllowedProviderPipeID). Caller-side gaps wired too: copilot dynamic headers for github-copilot (TestResponsesCopilotDynamicHeaders) and cloudflare resolveCloudflareBaseURL + cf-aig-authorization + suppressed Authorization (TestResponsesCloudflareAIGateway, TestResponsesCloudflareMissingEnvFailsStream) — closes the C4/C8 "responses caller" remainders.*
- [x] **D7 LOW** error msg format `(429):` vs `429:` + code suffix + 600B truncation (can split UTF-8); max_output_tokens 0 sent; invalid thinkingSignature silently dropped vs throw; completePartialJSON trailing-comma-in-string; function_call done-without-added dropped; null response.completed skipped; ~~sharedClient nil-deref if DefaultTransport replaced~~ *(fixed in retry.go: falls back to a fresh default-mirroring &http.Transport{} when the type assertion fails; TestSharedClientSurvivesReplacedDefaultTransport)*; sanitizeSurrogates FFFD vs delete. *All fixed: (a) responses-specific formatResponsesHTTPError porting formatOpenAIResponsesError + openai@6 APIError.makeMessage (`OpenAI API error (429): 429 msg`, code-suffix and 600B truncation dropped; shared formatProviderError truncation also dropped — pi has none; TestResponsesHTTPErrorFormat, TestOpenAISDKErrorMessage); (b) max_output_tokens omitted for 0 (TestResponsesMaxTokensZeroOmitted); (c) invalid thinkingSignature fails the stream with the bare parse error (TestResponsesInvalidThinkingSignatureFailsStream); (d) completePartialJSON: whole-input trim first (partial-json semantics), comma-strip only outside strings, dangling object-key strings dropped (TestParseStreamingJSONTrailingCommaInsideString, partial-json 0.1.7-verified); (e) function_call done-without-added constructs + emits toolcall_end, not landed in content like pi (TestResponsesFunctionCallDoneWithoutAdded); (f) null response.completed still runs cost calc/mapStopReason/toolUse promotion (TestResponsesCompletedNullResponse); (g) sanitizeSurrogates DELETES unpaired WTF-8 surrogates, preserves pairs (TestSanitizeSurrogatesDeletesUnpaired); (h) OnPayload error fails stream + wholesale replacement, OnResponse error fails stream (TestResponsesOnPayloadOnResponsePropagation).*

## E. Anthropic

- [x] **E1 HIGH cloudflare-ai-gateway branch missing** — cf-aig-authorization + null x-api-key + resolveCloudflareBaseUrl (anthropic.ts:802-820). (Shares C4 resolver.) *Fixed: cloudflare branch first in applyAnthropicHeaders (cf-aig-authorization Bearer, no x-api-key/authorization), baseURL via resolveCloudflareBaseURL with missing-env stream failure; TestAnthropicCloudflareAIGateway, TestAnthropicCloudflareMissingEnvFailsStream.*
- [x] **E2 MED copilot dynamic headers** (= C8 for anthropic.ts:488-494). *Fixed: buildCopilotDynamicHeaders merged after model.Headers, before opts.Headers (pi position); TestAnthropicCopilotDynamicHeaders.*
- [x] **E3 MED thinking tri-state** — ThinkingEnabled bool forces `thinking:{disabled}` when unset; pi omits key unless explicitly false. Add Provided flag (like google). *Fixed: AnthropicOptions.ThinkingProvided gates the thinking key (unset → omitted; Provided+false → {type:disabled}; Provided+true → enabled/adaptive); StreamSimpleAnthropic always sets Provided (reasoning-off still explicit-disable like pi streamSimple); registry adapter leaves it unset; temperature suppressed only when Provided+Enabled. Tests: TestAnthropicThinkingOmittedWhenNotProvided/ExplicitFalseSendsDisabled/EnabledSendsBudget, TestAnthropicStreamSimpleNoReasoningDisablesThinking.*
- [x] **E4 MED x-session-affinity not suppressed when cacheRetention none** (openai.go got it right; anthropic missed). *Fixed: header dropped when effective resolveCacheRetention is none (pi anthropic.ts:496-497); TestAnthropicSessionAffinityRetention.*
- [x] **E5 LOW** delta-vs-block-type guard; bare-CR SSE; oauth sniff before provider branches. *Fixed: (a) text/thinking/input_json/signature deltas only apply when the indexed block kind matches, else dropped silently (TestAnthropicMismatchedDeltaDroppedSilently); (b) scanSSELines SplitFunc accepts \r, \n, \r\n (TestAnthropicBareCRSSE); (c) OnPayload error fails the stream pre-request (TestAnthropicOnPayloadErrorFailsStream); cloudflare/copilot branches now precede the OAuth sniff so sk-ant-oat keys can't hijack those providers (covered by E1/E2 tests).*

## F. Google

- [x] **F1 HIGH gemma-4 thinkingLevel map** — pi: minimal/low→MINIMAL, medium/high→HIGH (google.ts:441-450); ours generic map. *Fixed: googleThinkingLevel adds the gemma-4 branch after gemini-3-pro, exact-case switches (xhigh → "" like pi's undefined); TestGoogleThinkingLevelMaps (per level × gemma-4/3-pro/3-flash).*
- [x] **F2 HIGH text-part presence** — Text must be *string; signature-only/inlineData parts currently fabricate empty text blocks; text+functionCall part drops text (pi independent ifs, `text !== undefined`). *Fixed: googlePart.Text is *string; loop runs pi's independent `text !== undefined` then `functionCall` checks in order (no continue) — signature-only parts produce nothing, text+functionCall produce both, "" still opens a block; TestGoogleSignatureOnlyPartProducesNothing, TestGoogleTextAndFunctionCallSamePart, TestGoogleEmptyStringTextPartIsPresent.*
- [x] **F3 MED error chunks swallowed** — `data:{"error":{...}}` + truncated stream → clean done; pi SDK throws (ApiError, incomplete-JSON). Add error field + post-loop guard. *Fixed: googleChunk.Error {code,message,status}; 400≤code<600 fails with the genai ApiError surface "got status: <status>. <chunk json>" (data-prefixed and bare-JSON chunks both); iterateGoogleSSE now splits on the SDK's \n\n,\r\r,\r\n\r\n delimiters and a trailing unconsumed segment fails with "Incomplete JSON segment at the end"; non-data lines still skipped. TestGoogleErrorChunkFailsStream, TestGoogleBareJSONErrorChunkFailsStream, TestGoogleTruncatedStreamFails.*
- [x] **F4 LOW** toolCall thoughtSignature missing from start/delta partials; empty generationConfig omitted vs always-sent {}; xhigh coercion. *Fixed: (a) toolCallSigs set before materialize/toolcall_start so partials carry thoughtSignature (TestGoogleToolCallSignatureOnStartPartial); (b) generationConfig always sent, {} when empty (TestGoogleGenerationConfigAlwaysSent); (c) only off→high coercion remains — xhigh falls through googleThinkingLevel ("") and googleBudget (*int nil for 2.5 families, -1 default for unknown families) yielding bare {includeThoughts:true} (TestGoogleXHighNoLevelNoBudget).*

## G. ai core

- [x] **G1 HIGH coercion accepts Infinity/NaN/hex/underscores** — schema.go ParseFloat vs JS Number(): reject non-finite (Number.isFinite/isInteger), trim before parse (JS trims), reject "1_0", accept "0x10"… match JS Number() semantics exactly; also matchesJSONType must reject NaN/Inf. *Fixed: jsNumber() ports ECMA-262 StringToNumber (JS-whitespace trim incl. FEFF / excl. NEL, hex/bin/oct via big.Int, strict decimal grammar, sign rules) gated by isFinite/isInteger; matchesJSONType rejects NaN/±Inf for number+integer (TypeBox IsNumber = Number.isFinite). ~40 node-verified vectors locked in schema_test.go.*
- [x] **G2 MED unenforced keywords** — pattern/minItems/maxItems/multipleOf/const/exclusiveMin/Max land in Extra, never validated; TypeBox enforces. Port enforcement for these. *Fixed: promoted to real Schema fields (marshal/unmarshal round-trip kept) and enforced; multipleOf ports TypeBox Guard.IsMultipleOf (1e-10 tolerance); RE2-uncompilable JS patterns (lookaround) skipped without error; messages exact-match TypeBox en_US locale ("must be >= 5", "must not have fewer than 2 items", …) — existing type/required/enum/anyOf/oneOf/additionalProperties wording realigned too (was "Expected …"/"Required"; required is now one error listing all missing props, pathed at the first, like pi's formatValidationPath).*
- [x] **G3 MED unknown-api throw-vs-stream** — pi stream() throws synchronously pre-stream; ours encodes in stream. Decide: match pi (return error from StreamSimple? Go shape) — at minimum document; prefer error return. *Documented as deliberate divergence on Stream/StreamSimple doc comments (stream.go); behavior unchanged per task decision.*
- [x] **G4 LOW** End() then End(result) drops result; UserMessage string-content not preserved on marshal + missing content key errors; getApiProviders() enumerator absent. *Fixed: End drops its done-guard (result still resolve-once; terminal event wins); UserMessage remembers string-form content and re-emits the string on marshal, missing/null content tolerated; GetApiProviders() added (sorted by Api for determinism). All test-locked.*

## H. Coding tools

- [x] ✓v **H1 HIGH edit prepareArguments dead** — runs inside Execute AFTER validation; harness has PrepareArguments hook pre-validation (loop.go:560) — set it on the tool; stringified edits/legacy oldText currently fail validation.
- [x] **H2 HIGH shell selection** — $SHELL/cmd vs pi bash-always (/bin/bash → which bash → sh; Windows Git Bash or error, never cmd) (shell.ts:66-109).
- [x] **H3 HIGH negative limit panics run** — find slice bounds, read negative end; clamp like pi (Math.min/slice semantics); tool panic shouldn't kill run anyway (B1 fixes blast radius; still clamp).
- [x] **H4 MED gitignore matcher gaps** — no `**` crossing /, dir-only pattern matches nested files (carve-out bug), no .git/info/exclude or global excludes; grep must not hard-ignore .git/node_modules when --hidden equiv (rg semantics: .git only via hidden rules; node_modules only if gitignored; gitignore only inside repos for grep — find keeps no-require-git).
- [x] **H5 MED grep glob anchoring** — rg anchors patterns containing '/' (no **/ prepend); fd semantics only for find.
- [x] **H6 MED bash output contract** — "(no output)" on nonzero-exit error text; signal-killed (exitCode null) = success w/ output not error; fractional timeout (float seconds, 0.5 must work, %g in message); limit ≤0 grep clamp 1; .log not .txt tempfile.
- [x] **H7 MED find: dirs never matched** (fd matches dirs; pi strips trailing slash); brace {a,b} + [!x] + smart-case in glob; binary-file skip in grep (NUL sniff).
- [x] **H8 MED bash memory** — full-stream bytes.Buffer + O(n) re-truncate per update → quadratic/OOM on huge output; pi keeps rolling tail + incremental temp-file writes. Port OutputAccumulator shape.
- [x] **H9 MED ls localeCompare ordering** — dotfiles/punct differ from ToLower+'<'; approximate localeCompare (case-insensitive, punct-aware) or document.
- [x] **H10 LOW** ENOENT error-code wording for edit; read-of-dir EISDIR text; details payloads (truncation/diff/patch) absent; non-vision note; find limit=0 unlimited; over-limit subset (traversal-first vs sorted-first); leading-/ pattern; TruncateLine UTF-16; trimEnd FEFF/0085; mutationqueue map growth + realpath err swallow; sniff io.ReadFull; grep 8MB line cap; throttle trailing-edge + initial empty update; abort-vs-timeout precedence; EXIT_STDIO_GRACE 100ms vs 2s.

### H fix notes (this sweep)
- H1: `PrepareArguments: prepareEditArguments` set on the edit tool; in-Execute call removed. Tests run the harness pipeline (PrepareArguments → ValidateToolArguments → Execute).
- H2: `getShellConfig()` ports shell.ts:57-110 (unix: /bin/bash → bash on PATH → sh; windows: Git Bash probes → bash.exe on PATH → pi's exact error; never $SHELL/cmd). `exec.LookPath` stands in for `which`/`where`+exists. Tested with fake PATH.
- H3: read implements pi's JS slice semantics (negative end counts from array end; raw userLimitedLines drives the footer math — limit=-1 yields offset=0 footer like pi). find: limit<=0 = unlimited (fd --max-results 0); limit=0 still emits pi's emergent "0 results limit reached" notice; negative limit emits no notice (fd would actually error on a negative count — deliberate small divergence). grep: Math.max(1, limit) clamp.
- H4: full rewrite — `**` crosses slashes, gitignore-spec anchoring, dir-only patterns no longer match nested plain files (carve-out bug fixed), `.git/info/exclude` + global core.excludesFile (`git config --path --get core.excludesFile`, XDG/~ fallback) + ancestor .gitignores apply when inside a repo. grep applies gitignore ONLY inside a repo; find keeps no-require-git. node_modules hard-ignore dropped for both (only if gitignored); `.git` itself always skipped (per tracker note; NB local rg 15.1 with --hidden actually descends .git — kept the skip as instructed). Old TestGrepGitignore pinned the no-repo bug; updated to a repo + new non-repo test.
- H5: grep uses matchRgGlob (slash patterns anchored to search root, no **/ prepend, case-sensitive); find keeps fd semantics (basename default, **/ prepend, --full-path absolute matching incl. leading-/ patterns).
- H6: "(no output)" substitution in nonzero-exit error text; signal-killed (Go ExitCode -1 ≙ pi exitCode null) = success; fractional timeout (float64, message prints raw value e.g. "0.5"); temp file `pi-bash-<16hex>.log`.
- H7: find matches directories; brace alternation {a,b}, [!x] classes, smart-case in the fd matcher; grep skips binary files via NUL sniff in the first 8KB (directory mode only; explicit files still searched, rg-verified).
- H8: outputAccumulator port (rolling tail ≤ 2×(2×50KB) + incremental temp-file streaming, lazily opened on first overflow); throttled updates with trailing-edge flush + initial empty onUpdate. Memory bound + tail correctness test-locked.
- H9: ls sorts with x/text collate language.Und on ToLower'd names — exactly reproduces node's `toLowerCase().localeCompare` on the verified sample (_x < .gitignore < ax, plus punctuation/digit/accent cases).
- H10: edit ENOENT wording; read-of-dir EISDIR text; details.truncation (+entryLimitReached/resultLimitReached/matchLimitReached/linesTruncated/fullOutputPath) for read/ls/find/grep/bash with pi JSON field shape; mutationqueue drains entries + propagates non-ENOENT/ENOTDIR realpath errors; image sniff io.ReadFull; grep line cap removed (whole-file read like pi's getFileLines); abort-before-timeout precedence; WaitDelay 100ms (EXIT_STDIO_GRACE); TruncateLine UTF-16 (incl. split-surrogate → U+FFFD); trimEnd JS whitespace set (FEFF in, 0085 out). SKIPPED: edit diff/patch details (no Go diff helpers yet — not cheap); non-vision image note (Execute has no model ctx); find over-limit subset is now traversal-first capped then sorted for output (fd-ish; output ordering stays the documented deliberate sort).
- I1 (tools side): PromptGuidelines added to read/edit/write byte-exact from the npm build; session-side collection now done (see I1 below).

## I. Session/SDK/prompt

- [x] **I1 HIGH per-tool promptGuidelines missing** — read/edit/write define 6 bullets pi injects into Guidelines (agent-session.ts:896-929); add PromptGuidelines to tool defs + collect in NewSession; fix golden. *Fixed: NewSession collects guidelines via collectPromptGuidelines (per-tool trim+dedupe like _normalizePromptGuidelines, tool order; cross-tool dedupe stays in the builder like pi) and passes them to BuildSystemPrompt. Position verified: conditional "Use bash…" bullet first, then tool guidelines in active-tool order, then the two static bullets. Golden updated to the full 9-bullet text and re-verified byte-for-byte against the npm build by invoking the built buildSystemPrompt with createAllToolDefinitions' snippets+guidelines for [read,bash,edit,write]. Tests: TestDefaultSystemPromptGolden (now built from resolveTools+collectPromptGuidelines like NewSession), TestNewSessionCollectsPromptGuidelines.*
- [x] **I2 HIGH custom SystemPrompt skips assembly** — pi appends context files/skills/date/cwd to custom prompts too (system-prompt.ts:53-80); route custom through BuildSystemPrompt with discovery. *Fixed: NewSession always routes through BuildSystemPrompt (CustomPrompt=opts.SystemPrompt) with discovery performed; custom path appends appendSection, project_context, skills (read-gated), date, cwd — never the docs block or Guidelines (verified in source + build; custom-path output verified byte-for-byte against the built buildSystemPrompt). SelectedTools passed as a concrete possibly-empty list so NoToolsAll doesn't fall back to the builder default. Tests: TestCustomSystemPromptAssemblyGolden (build-verified golden), TestNewSessionCustomPromptStillAssembles (AGENTS.md + skill + order + banned sections).*
- [x] **I3 MED summary text blocks joined without \n** (compaction.js join("\n")). *Fixed in completeSummarization: text blocks collected and strings.Join("\n"); TestSummaryTextBlocksJoinedWithNewline.*
- [x] **I4 MED usage estimate counts aborted/error assistants** (pi skips both). *Fixed: estimateContextTokensUsageAware skips StopAborted/StopError when picking the anchor (pi getAssistantUsage); TestUsageEstimateSkipsAbortedAndError.*
- [x] **I5 MED cut snap direction** — pi snaps FORWARD (first cutPoint ≥ i, toolResult goes into summary); ours snaps backward. *Fixed: findCutIndex replaced by findCutPoint porting pi findValidCutPoints+findCutPoint (compaction.ts:380-448) over the flat message list: cut points = non-toolResult indices, forward snap to first cutPoint ≥ crossing index, default cutPoints[0] keep-everything edge, split-turn detection (turnStartIndex = nearest preceding user). The non-message backward scan (ts:424-437) has no equivalent — every element at this layer is a message. Old TestFindCutIndexSkipsToolResult pinned the backward-snap bug; replaced by TestFindCutPointSnapsForward (boundary toolResult now summarized — cut moves 1→3 on the same fixture) + TestFindCutPointKeepEverythingEdge + TestFindCutPointSplitTurn. TestCompactionSummarizesAndKeepsRecent's keepRecentTokens bumped 400→1500: under faithful cut semantics 400 produced a split turn; the split path has its own test.*
- [x] **I6 MED no UPDATE_SUMMARIZATION_PROMPT / split-turn path** — re-compaction quality; port previous-summary flow (turn-prefix flow optional, note if deferred). *Fixed: UPDATE_SUMMARIZATION_PROMPT + TURN_PREFIX_SUMMARIZATION_PROMPT byte-exact from the build (verified by containment check against compaction.js template literals). generateSummary takes previousSummary → <previous-summary> tags + update prompt; state.summary stores the file-ops appendix so previousSummary includes it (pi persists it that way). Split-turn flow ported at the message level (cut on non-user → summarize [prefix,turnStart) with 0.8 budget + [turnStart,cut) with TURN_PREFIX prompt at 0.5 budget, "No prior history." fallback, pi's exact merge separator); the two summaries run sequentially (pi: Promise.all parallel — same output, deterministic tests). File-ops carry across compactions via state.readFiles/modifiedFiles seeding fileOps.read/edited (pi extractFileOperations from prev compaction details). Tests: TestCompactionExtendsWithPreviousSummary, TestCompactionSplitTurnSummaries.*
- [x] **I7 MED summarization opts drop reasoning + headers** (pi passes thinkingLevel + headers). *Fixed: completeSummarization passes Headers: s.Agent.Headers (the session's configured headers) and Reasoning = session thinking level when model.Reasoning && level set && != off (pi createSummarizationOptions); TestSummarizationPassesReasoningAndHeaders (medium passed, off omitted, headers forwarded).*
- [x] **I8 MED custom tools bypass allow/exclude/noTools** — pi filters ALL tools through allowlist/denylist; noTools all disables custom too. *Fixed: resolveTools ports sdk.ts:245 (allowlist = ToolNames ?? (NoToolsAll ? empty : nil)) + agent-session.ts:2285-2298 isAllowedTool over built-ins AND customs; initial active = tools ?? (noTools ? [] : default) minus excludes; with an allowlist every registry tool in it activates (so ToolNames can enable grep/find/ls and custom tools), without one all surviving customs activate (includeAllExtensionTools); customs override same-named built-ins. Verbatim Tools plays baseToolsOverride (its names = default active, still filtered). Corrected test: TestSessionNoToolsAll previously pinned the append-always bug (expected custom to survive noTools=all) — now expects an empty tool set. New: TestSessionNoToolsBuiltinKeepsCustom, TestSessionAllowlistConstrainsCustom, TestSessionExcludeAppliesToCustom.*
- [x] **I9 MED bashExecution/custom-role session entries dropped on resume** — UnmarshalMessage fails → entry skipped; pi renders "Ran `cmd`…" user text. Tolerate custom roles in session reader (parse as raw → convertToLlm-equivalent). *Fixed: session_tree.go unmarshalSessionMessage handles the AgentMessage union per messages.ts convertToLlm: bashExecution → "Ran `cmd`…" user text (bashExecutionToText port incl. (no output)/cancelled/exit-code/truncated variants; excludeFromContext skipped), custom → content as user, branchSummary/compactionSummary → existing wrapper helpers; unknown roles still drop. New sessparity fixture 8_agent_message_roles.json (bashExecution w/ exit 2 + truncation, excluded bashExecution, custom string content) with golden generated from the npm build via /tmp/pi-diff/pi_session.mjs.*
- [x] **I10 MED thinking_level_change not recorded at StartSession** (pi writes model_change + thinking_level_change). *Fixed: StartSession takes a variadic thinkingLevel (existing callers untouched) and records thinking_level_change after model_change (sdk.ts:362-373); cmd/pi passes the session's clamped level. Test: TestStartSessionRecordsThinkingAfterModel (entry order session→model_change→thinking_level_change→message).*
- [x] **I11 MED skills sorted by name vs discovery order; frontmatter not YAML (multiline desc breaks; disable-model-invocation strict bool)** — minimal YAML-ish upgrade for block scalars or vendored mini-parser. *Fixed: name sort dropped (pi: insertion-ordered Map — user dir then project; within a dir Go's os.ReadDir is filename-sorted vs node fs order — deliberate, deterministic). parseFrontmatter upgraded to a typed scalar subset: block scalars |,>,|-,>-,|+,>+ (clip/strip/keep chomping, folding), multi-line plain scalars folded, quoted scalars (kind-tracked, '' and minimal \"-escapes), ` #` comment stripping. Unsupported (documented in code): nested mappings, sequences, flow collections, anchors/tags, explicit indent indicators, more-indented literal lines inside folded scalars. disable-model-invocation: YAML boolean true only (plain true/True/TRUE; quoted "true" is a string → not disabled, pi `=== true`). Tests: TestLoadSkillsPreservesDiscoveryOrder, TestSkillBlockScalarDescription, TestParseFrontmatterBlockScalars, TestParseFrontmatterMultilinePlain, TestSkillDisableModelInvocationStrictBool.*
- [x] **I12 MED model resolution** — provider-prefix only if known provider (openrouter ids contain /); case-insensitive; :thinkingLevel suffix; pi error text. *Fixed: resolve.go ports resolveCliModel + parseModelPattern + tryMatchModel + findExactModelReferenceMatch + isAlias + buildFallbackModel + defaultModelPerProvider (build-verified): slash prefix is a provider only when known (else whole string matches as a model id — OpenRouter ids); inferred-provider miss falls back to the full input across all models; case-insensitive everywhere; :level suffix (off|minimal|low|medium|high|xhigh, VALID_THINKING_LEVELS) parsed in strict mode (allowInvalidThinkingLevelFallback=false) and returned via ResolveModelPattern → ResolvedModel{Model,ThinkingLevel,Warning}; unknown-under-known-provider synthesizes a custom-id clone with pi's warning; error text byte-exact `Model "<x>" not found. Use --list-models to see available models.`. ResolveModel keeps the old 2-value signature. DELIBERATE: empty spec → DefaultModelSpec (pi's default comes from settings/findInitialModel; no settings manager here); catalog iterated provider+id sorted (pi: models.json order); alias tie-break uses Go byte compare vs JS localeCompare (ASCII ids — same result). Tests: TestResolveModelOpenRouterSlashedID, TestResolveModelProviderPrefixFallsBackToFullID, TestResolveModelCaseInsensitive, TestResolveModelThinkingLevelSuffix, TestResolveModelUnknownErrorText, TestResolveModelCustomIDFallback.*
- [x] **I13 LOW** truncateForSummary byte-slice vs UTF-16 (rune-safe at least); StopAborted treated as summarize-failure; flush O_EXCL; project_instructions %q escaping; cmd/pi: compaction not enabled by default, --resume forks new file instead of appending, thinking/model not restored. *Compaction items DONE: truncateForSummary counts/slices UTF-16 code units (sliceUTF16; a boundary surrogate pair is dropped whole rather than split like JS slice would — deliberate, keeps valid UTF-8; TestTruncateForSummaryUTF16); StopAborted no longer fails summarization — pi throws only on "error" (compaction.js:466), aborted returns the text produced so far (TestSummarizationAbortedReturnsPartialText). Remaining items DONE: (a) first session flush opens O_CREATE|O_EXCL (pi "wx"; TestFirstFlushExclusiveCreate — pre-existing file never clobbered); (b) project_instructions path interpolated raw (`path="%s"`, pi `path="${filePath}"` — no %q escaping); (c) skill name/description lengths validated in UTF-16 code units via utf16Len (TestSkillValidationLengthsUTF16); (d) cmd/pi: compaction enabled by default (&DefaultCompactionSettings, pi default-on), --resume/-c APPEND to the resumed file via new ResumeSession (port of setSessionFile: same header/id, leaf = file's last entry, flushed=true so appends are immediate; ids seeded for collision checks; TestResumeSessionAppendsToSameFile), and model/thinking restored from BuildContext (model only when -m absent; thinking priority --think > :suffix > recorded session level, with pi's append-thinking_level_change-when-missing on resume). ResumeSession errors on a header-less file instead of pi's truncate-and-restart (that path serves pi's explicit --session flag, which the Go CLI doesn't have).*

## Cross-cutting fixes (dedupe)
- [x] cloudflare resolveBaseUrl util (C4/E1 + responses caller) — *util landed: ai/providers/cloudflare.go (resolveCloudflareBaseURL, isCloudflareProvider, 4 baseURL consts; exact pi error message; empty env counts as unset). Completions + responses callers wired; E1 (anthropic) still pending.*
- [x] copilot dynamic headers util (C8/E2 + responses caller) — *util landed: ai/providers/copilot.go (inferCopilotInitiator, hasCopilotVisionInput, buildCopilotDynamicHeaders; user/toolResult-only image scan, value+pointer message variants). Completions + responses callers wired; E2 (anthropic) still pending.*
- OnPayload/OnResponse error propagation (all providers) (C10)
- [x] retry default semantics *int MaxRetries (D1 + L3 completions + A6 anthropic-google note) — *done in retry.go via D1 (int zero-value == pi default 0, no *int needed); all four providers pick it up through retryFromOptions.*
- prompt_cache_retention sessionID gate (C5 both OpenAI providers)

## Suggested fix order
1. A* regressions + B1/B2 (agent+compaction+events+completions-stream) — crash/hang/wrong-context grade
2. C1-C4, D1-D2, E1, F1-F2, G1, H1-H3, I1-I2 (remaining HIGHs)
3. MED clusters by file (compaction I3-I7, sdk I8-I12, bash H6/H8, glob H4-H5/H7, providers C5-C10/D3-D6/E2-E4/F3, core G2-G3)
4. LOW sweep last, skip any that risk regressions; document deliberate non-ports.
